class Rectangle
    def print_area_and_parameter()
        area = width * length
        puts "The area of the rectangle is #{temp}}"
        parameter = (@width * 2) + (@length * 2)
        puts "The perimeter of the rectangle is #{temp}"
    end
end